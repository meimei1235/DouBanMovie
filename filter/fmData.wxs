function fmActor(list) {
	var list = list.slice(1);
	var arr = [];
	if(list.length > 0) {
		list.forEach(function(item){
			arr.push(item.name);
		})
	}
	return arr.join(',');
}

module.exports = {
	fmActor: fmActor
}